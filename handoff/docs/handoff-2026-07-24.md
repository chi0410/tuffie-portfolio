# 專案內頁交辦指令｜2026-07-24

共兩項。

---

## 1. Next Project 區塊調整

適用範圍：專案一、專案二內頁

1. **區塊上方加一條水平分隔線**
   - 使用 design tokens 既有的 border／divider 顏色，1px hairline
   - 寬度對齊 1120px 容器
   - 分隔線與上一個區塊間距 120px（沿用三欄間距規則）

2. **「Next Project」標題移出卡片**
   - 現況：標題在卡片內部
   - 改為：標題獨立放在分隔線下方、卡片上方
   - 標題與卡片間距 24px

3. **卡片本體改用首頁專案卡格式**
   - 直接沿用首頁專案卡的既有 component／class，不要另外寫一套
   - 縮圖比例、圓角、hover 行為、標籤樣式全部與首頁一致

4. **專案二的 Next Project 指向專案一**
   - 連結導向 case study 01（跨裝置跨系統同步管理）
   - 卡片內容（縮圖、標題、標籤）同步換成專案一的資料

---

## 2. design-tokens.md 圓角段落校正

1. 掃描專案內所有 CSS，列出實際使用中的 `border-radius` 值及各自的使用對象（卡片、按鈕、輸入框、標籤、圖片容器等）
2. 對照 `design-tokens.md` 圓角段落，找出所有不符的項目
   - 已知：文件寫卡片 12px，實際使用 16px
3. 更新文件為實際值。若同類元件出現多種圓角值，先列出來給設計端看，不要自行決定要統一成哪個
4. 只改圓角段落，不要動 CSS 本身
